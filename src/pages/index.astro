---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { Icon } from 'astro-icon/components';
import { SITE_TITLE, SITE_DESCRIPTION, CALCULATORS, CALCULATORS_DINAMIC, SIMULATORS_CINEMATICA } from '../consts';
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<meta name="keywords" content="física, calculadoras, simuladores, movimiento, cinemática, dinámica, educación, estudiantes">
		<meta name="author" content="PhSk Platform">
		<meta property="og:title" content={SITE_TITLE}>
		<meta property="og:description" content={SITE_DESCRIPTION}>
		<meta property="og:type" content="website">
		<meta name="twitter:card" content="summary_large_image">
		<meta name="twitter:title" content={SITE_TITLE}>
		<meta name="twitter:description" content={SITE_DESCRIPTION}>
	</head>
	<body>
		<Header />
		<main>
			<!-- Hero Section -->
			<section class="hero">
				<div class="hero-content">
					<div class="hero-text">
						<h1>¡Domina la Física con PhSk!</h1>
						<p class="hero-subtitle">
							Descubre los secretos del universo con nuestras calculadoras interactivas y simuladores avanzados
						</p>
						<div class="hero-buttons">
							<a href="/calculator" class="btn btn-primary">
								<Icon name="mdi:calculator" size="20" />
								Explorar Calculadoras
							</a>
							<a href="/simulators" class="btn btn-secondary">
								<Icon name="mdi:play-circle" size="20" />
								Iniciar Simuladores
							</a>
						</div>
						<div class="hero-stats">
							<div class="stat">
								<span class="stat-number">{CALCULATORS.length + CALCULATORS_DINAMIC.length}</span>
								<span class="stat-label">Calculadoras</span>
							</div>
							<div class="stat">
								<span class="stat-number">{SIMULATORS_CINEMATICA.length}</span>
								<span class="stat-label">Simuladores</span>
							</div>
							<div class="stat">
								<span class="stat-number">100%</span>
								<span class="stat-label">Gratuito</span>
							</div>
						</div>
					</div>
					<div class="hero-visual">
						<div class="physics-animation">
							<div class="orbit">
								<div class="planet"></div>
							</div>
							<div class="particles">
								<div class="particle"></div>
								<div class="particle"></div>
								<div class="particle"></div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- Features Section -->
			<section class="features">
				<div class="container">
					<h2>¿Por qué elegir PhSk?</h2>
					<div class="features-grid">
						<div class="feature">
							<div class="feature-icon">
								<Icon name="mdi:target" size="48" />
							</div>
							<h3>Simuladores Interactivos</h3>
							<p>Visualiza conceptos físicos complejos con nuestros simuladores en tiempo real powered by PixiJS</p>
						</div>
						<div class="feature">
							<div class="feature-icon">
								<Icon name="mdi:lightning-bolt" size="48" />
							</div>
							<h3>Cálculos Precisos</h3>
							<p>Resuelve problemas de física con calculadoras especializadas y algoritmos optimizados</p>
						</div>
						<div class="feature">
							<div class="feature-icon">
								<Icon name="mdi:devices" size="48" />
							</div>
							<h3>100% Responsive</h3>
							<p>Accede desde cualquier dispositivo - móvil, tablet o desktop</p>
						</div>
						<div class="feature">
							<div class="feature-icon">
								<Icon name="mdi:rocket-launch" size="48" />
							</div>
							<h3>Aprendizaje Activo</h3>
							<p>Experimenta con diferentes valores y observa los resultados instantáneamente</p>
						</div>
					</div>
				</div>
			</section>

			<!-- Physics Concepts Section -->
			<section class="physics-concepts">
				<div class="container">
					<h2>Explora el Mundo de la Física</h2>
					<p class="section-subtitle">
						La física es la ciencia que estudia cómo funciona el mundo. Desde la caída de una manzana hasta cómo brillan las estrellas en el cielo.
					</p>
					<div class="concepts-grid">
						<div class="concept">
							<div class="concept-icon">
								<Icon name="mdi:run" size="40" />
							</div>
							<h3>Movimiento</h3>
							<p>Comprende por qué los autos se detienen cuando frenas y cómo calculamos velocidades y aceleraciones</p>
						</div>
						<div class="concept">
							<div class="concept-icon">
								<Icon name="mdi:flash" size="40" />
							</div>
							<h3>Energía</h3>
							<p>Descubre cómo la electricidad llega a tu casa y las diferentes formas de energía</p>
						</div>
						<div class="concept">
							<div class="concept-icon">
								<Icon name="mdi:earth" size="40" />
							</div>
							<h3>Gravedad</h3>
							<p>Entiende por qué no flotamos como astronautas en el espacio</p>
						</div>
						<div class="concept">
							<div class="concept-icon">
								<Icon name="mdi:lightbulb" size="40" />
							</div>
							<h3>Luz y Sonido</h3>
							<p>Aprende cómo vemos colores y escuchamos música a través de ondas</p>
						</div>
					</div>
				</div>
			</section>

			<!-- Popular Tools Section -->
			<section class="popular-tools">
				<div class="container">
					<h2>Herramientas Más Populares</h2>
					<div class="tools-grid">
						<!-- Cinemática Tools -->
						<div class="tool-category">
							<h3><Icon name="mdi:motion" size="24" /> Cinemática</h3>
							<div class="tools-list">
								{CALCULATORS.slice(0, 3).map((calc) => (
									<a href={calc.url} class="tool-card">
										<div class="tool-info">
											<h4>{calc.name}</h4>
											<p>{calc.description}</p>
										</div>
										<div class="tool-arrow">→</div>
									</a>
								))}
							</div>
						</div>
						
						<!-- Dinámica Tools -->
						<div class="tool-category">
							<h3><Icon name="mdi:flash" size="24" /> Dinámica</h3>
							<div class="tools-list">
								{CALCULATORS_DINAMIC.map((calc) => (
									<a href={calc.url} class="tool-card">
										<div class="tool-info">
											<h4>{calc.name}</h4>
											<p>{calc.description}</p>
										</div>
										<div class="tool-arrow">→</div>
									</a>
								))}
							</div>
						</div>

						<!-- Simuladores -->
						<div class="tool-category">
							<h3><Icon name="mdi:gamepad-variant" size="24" /> Simuladores</h3>
							<div class="tools-list">
								{SIMULATORS_CINEMATICA.slice(0, 3).map((sim) => (
									<a href={sim.url} class="tool-card">
										<div class="tool-info">
											<h4>{sim.name}</h4>
											<p>{sim.description}</p>
										</div>
										<div class="tool-arrow">→</div>
									</a>
								))}
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- CTA Section -->
			<section class="cta">
				<div class="container">
					<div class="cta-content">
						<h2>¿Listo para Dominar la Física?</h2>
						<p>Únete a miles de estudiantes que ya están aprendiendo física de manera interactiva</p>
						<div class="cta-buttons">
							<a href="/calculator" class="btn btn-primary btn-large">
								<Icon name="mdi:calculator" size="24" />
								Comenzar con Calculadoras
							</a>
							<a href="/simulators" class="btn btn-outline btn-large">
								<Icon name="mdi:play-circle" size="24" />
								Probar Simuladores
							</a>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	/* Global Styles */
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
	}

	/* Hero Section */
	.hero {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		padding: 3rem 0;
		min-height: 100vh;
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
	}

	.hero-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1.5rem;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
	}

	.hero-text {
		z-index: 2;
	}

	.hero h1 {
		font-size: clamp(2rem, 5vw, 3.5rem);
		font-weight: 800;
		margin-bottom: 1rem;
		line-height: 1.2;
	}

	.hero-subtitle {
		font-size: clamp(1rem, 2.5vw, 1.25rem);
		margin-bottom: 2rem;
		opacity: 0.95;
		line-height: 1.5;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
		margin-bottom: 2.5rem;
		flex-wrap: wrap;
	}

	.btn {
		padding: 0.875rem 1.5rem;
		border-radius: 0.5rem;
		text-decoration: none;
		font-weight: 600;
		font-size: 0.95rem;
		transition: all 0.3s ease;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		border: 2px solid transparent;
		white-space: nowrap;
		min-width: fit-content;
	}

	.btn-primary {
		background: #fff;
		color: #667eea;
	}

	.btn-primary:hover {
		background: #f8fafc;
		transform: translateY(-2px);
		box-shadow: 0 10px 25px rgba(0,0,0,0.1);
	}

	.btn-secondary {
		background: transparent;
		color: white;
		border: 2px solid rgba(255,255,255,0.3);
	}

	.btn-secondary:hover {
		background: rgba(255,255,255,0.1);
		border-color: rgba(255,255,255,0.6);
		transform: translateY(-2px);
	}

	.btn-outline {
		background: transparent;
		color: #667eea;
		border: 2px solid #667eea;
	}

	.btn-outline:hover {
		background: #667eea;
		color: white;
		transform: translateY(-2px);
	}

	.btn-large {
		padding: 1rem 1.75rem;
		font-size: 1rem;
	}

	.hero-stats {
		display: flex;
		gap: 2rem;
		flex-wrap: wrap;
	}

	.stat {
		text-align: center;
		min-width: 80px;
	}

	.stat-number {
		display: block;
		font-size: clamp(1.5rem, 3vw, 2rem);
		font-weight: 800;
		color: #fbbf24;
	}

	.stat-label {
		font-size: clamp(0.8rem, 1.5vw, 0.875rem);
		opacity: 0.8;
	}

	/* Physics Animation */
	.hero-visual {
		display: flex;
		justify-content: center;
		align-items: center;
		z-index: 1;
	}

	.physics-animation {
		position: relative;
		width: clamp(200px, 25vw, 300px);
		height: clamp(200px, 25vw, 300px);
	}

	.orbit {
		position: absolute;
		width: 80%;
		height: 80%;
		border: 2px solid rgba(255,255,255,0.2);
		border-radius: 50%;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		animation: rotate 10s linear infinite;
	}

	.planet {
		position: absolute;
		width: clamp(15px, 2vw, 20px);
		height: clamp(15px, 2vw, 20px);
		background: #fbbf24;
		border-radius: 50%;
		top: -10px;
		left: 50%;
		transform: translateX(-50%);
		box-shadow: 0 0 20px rgba(251, 191, 36, 0.5);
	}

	.particles {
		position: absolute;
		width: 100%;
		height: 100%;
	}

	.particle {
		position: absolute;
		width: clamp(4px, 0.8vw, 6px);
		height: clamp(4px, 0.8vw, 6px);
		background: rgba(255,255,255,0.6);
		border-radius: 50%;
		animation: float 3s ease-in-out infinite;
	}

	.particle:nth-child(1) {
		top: 20%;
		left: 30%;
		animation-delay: 0s;
	}

	.particle:nth-child(2) {
		top: 60%;
		right: 20%;
		animation-delay: 1s;
	}

	.particle:nth-child(3) {
		bottom: 30%;
		left: 20%;
		animation-delay: 2s;
	}

	@keyframes rotate {
		from { transform: translate(-50%, -50%) rotate(0deg); }
		to { transform: translate(-50%, -50%) rotate(360deg); }
	}

	@keyframes float {
		0%, 100% { transform: translateY(0px); opacity: 0.6; }
		50% { transform: translateY(-20px); opacity: 1; }
	}

	/* Features Section */
	.features {
		padding: 5rem 0;
		background: #f8fafc;
	}

	.features h2 {
		text-align: center;
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 3rem;
		color: #1e293b;
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
	}

	.feature {
		background: white;
		padding: 2rem;
		border-radius: 1rem;
		text-align: center;
		box-shadow: 0 4px 6px rgba(0,0,0,0.1);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.feature:hover {
		transform: translateY(-5px);
		box-shadow: 0 20px 40px rgba(0,0,0,0.1);
	}

	.feature-icon {
		margin-bottom: 1rem;
		color: #667eea;
		display: flex;
		justify-content: center;
	}

	.feature-icon svg {
		width: 48px;
		height: 48px;
	}

	.feature h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
		color: #1e293b;
	}

	.feature p {
		color: #64748b;
		line-height: 1.6;
	}

	/* Physics Concepts Section */
	.physics-concepts {
		padding: 5rem 0;
		background: white;
	}

	.physics-concepts h2 {
		text-align: center;
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
		color: #1e293b;
	}

	.section-subtitle {
		text-align: center;
		font-size: 1.25rem;
		color: #64748b;
		margin-bottom: 3rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.concepts-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.concept {
		text-align: center;
		padding: 1.5rem;
		border-radius: 0.75rem;
		background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
		transition: transform 0.3s ease;
	}

	.concept:hover {
		transform: translateY(-3px);
	}

	.concept-icon {
		margin-bottom: 1rem;
		color: #667eea;
		display: flex;
		justify-content: center;
	}

	.concept-icon svg {
		width: 40px;
		height: 40px;
	}

	.concept h3 {
		font-size: 1.25rem;
		margin-bottom: 0.75rem;
		color: #1e293b;
	}

	.concept p {
		color: #64748b;
		font-size: 0.9rem;
		line-height: 1.5;
	}

	/* Popular Tools Section */
	.popular-tools {
		padding: 5rem 0;
		background: #f8fafc;
	}

	.popular-tools h2 {
		text-align: center;
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 3rem;
		color: #1e293b;
	}

	.tools-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 3rem;
	}

	.tool-category h3 {
		font-size: 1.5rem;
		margin-bottom: 1.5rem;
		color: #1e293b;
		text-align: center;
	}

	.tools-list {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.tool-card {
		background: white;
		padding: 1.5rem;
		border-radius: 0.75rem;
		text-decoration: none;
		color: inherit;
		display: flex;
		justify-content: space-between;
		align-items: center;
		transition: all 0.3s ease;
		border: 1px solid #e2e8f0;
	}

	.tool-card:hover {
		transform: translateX(5px);
		box-shadow: 0 10px 25px rgba(0,0,0,0.1);
		border-color: #667eea;
	}

	.tool-info h4 {
		font-size: 1.1rem;
		margin-bottom: 0.5rem;
		color: #1e293b;
	}

	.tool-info p {
		font-size: 0.875rem;
		color: #64748b;
		line-height: 1.4;
	}

	.tool-arrow {
		font-size: 1.5rem;
		color: #667eea;
		font-weight: bold;
	}

	/* CTA Section */
	.cta {
		padding: 5rem 0;
		background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
		color: white;
	}

	.cta-content {
		text-align: center;
	}

	.cta h2 {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
	}

	.cta p {
		font-size: 1.25rem;
		margin-bottom: 2rem;
		opacity: 0.9;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.hero {
			min-height: auto;
			padding: 2rem 0 3rem 0;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 2rem;
			padding: 0 1rem;
		}

		.hero-text {
			text-align: center;
			order: 2;
		}

		.hero-visual {
			order: 1;
			margin-bottom: 1rem;
		}

		.hero h1 {
			font-size: clamp(1.75rem, 6vw, 2.5rem);
			margin-bottom: 1rem;
		}

		.hero-subtitle {
			font-size: clamp(0.95rem, 3vw, 1.1rem);
			margin-bottom: 1.5rem;
		}

		.hero-buttons {
			justify-content: center;
			gap: 0.75rem;
			margin-bottom: 2rem;
			flex-direction: column;
			align-items: center;
		}

		.btn {
			width: 100%;
			max-width: 280px;
			justify-content: center;
			padding: 1rem 1.5rem;
			font-size: 0.9rem;
		}

		.hero-stats {
			justify-content: center;
			gap: 1.5rem;
		}

		.stat {
			min-width: 70px;
		}

		.physics-animation {
			width: 180px;
			height: 180px;
		}

		.tools-grid {
			grid-template-columns: 1fr;
		}

		.cta-buttons {
			flex-direction: column;
			align-items: center;
			gap: 1rem;
		}

		.btn-large {
			width: 100%;
			max-width: 300px;
			justify-content: center;
		}
	}

	@media (max-width: 480px) {
		.hero {
			padding: 1.5rem 0 2.5rem 0;
		}

		.hero-content {
			padding: 0 0.75rem;
			gap: 1.5rem;
		}

		.hero h1 {
			font-size: clamp(1.5rem, 7vw, 2rem);
			line-height: 1.3;
		}

		.hero-subtitle {
			font-size: clamp(0.9rem, 3.5vw, 1rem);
			line-height: 1.4;
		}

		.hero-buttons {
			gap: 0.5rem;
			margin-bottom: 1.5rem;
		}

		.btn {
			padding: 0.875rem 1.25rem;
			font-size: 0.85rem;
		}

		.hero-stats {
			gap: 1rem;
		}

		.stat-number {
			font-size: clamp(1.25rem, 4vw, 1.5rem);
		}

		.stat-label {
			font-size: clamp(0.7rem, 2vw, 0.8rem);
		}

		.physics-animation {
			width: 150px;
			height: 150px;
		}

		.features,
		.physics-concepts,
		.popular-tools,
		.cta {
			padding: 2.5rem 0;
		}

		.feature,
		.tool-card {
			padding: 1rem;
		}

		.container {
			padding: 0 0.75rem;
		}
	}

	/* Tool category icons */
	.tool-category h3 {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
	}
</style>